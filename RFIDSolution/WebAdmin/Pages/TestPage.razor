<Pagination @bind-TableState="state" StateChanged="OnPageClicked"></Pagination>

@attribute [Authorize]
@page "/test"
@code {
    TableState state = new TableState();

    protected override void OnInitialized()
    {
        base.OnInitialized();
    }

    private async Task OnPageClicked(TableState state)
    {

    }
}
